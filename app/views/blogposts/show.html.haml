.row
    .col-md-1
        = ""

    .col-md-8
        .page-header
            %h1
                %small=@blogpost.created_at.strftime("%Y-%m-%d")

        = markdoon @blogpost.body

        - if current_user && current_user.admin?
            = link_to 'Edit', edit_blogpost_path(@blogpost)

    .col-md-3
        = ""