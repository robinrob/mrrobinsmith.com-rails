<div class="page-header">
    <h1>Robin Smith Software Blog</h1>
</div>


<div class="row">
    <% @blogposts.each_with_index do |blogpost, i| %>
        <% if ((i % 2) == 0) and i != 0 %>
            </div>
        <% end %>
            <div class="col-md-6">
                <div class="page-header">
                    <h2><%= blogpost.title %>
                        <br>
                        <small><%= blogpost.created_at.strftime("%Y-%m-%d") %></small>
                    </h2>
                </div>

                <p>
                    <%= "#{blogpost.preview} ..." %>
                    <%= link_to 'Show', blogpost_path(blogpost) %></td>
                </p>
            </div>
        <% if ((i % 2) == 0) %>
            <div class="row">
        <% end %>
    <% end %>
</div>