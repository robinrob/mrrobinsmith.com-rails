<h1>Robin Smith Software Blog</h1>

<div class="span6">
    <%= form_for :blogpost, :url => blogposts_path do |f| %>

        <% @blogposts.each do |blogpost| %>
            <p>
                <h2><%= blogpost.title %></h2>
            </p>
            <p>
                <%= "#{blogpost.preview} ..." %>
                <%= link_to 'Show', blogpost_path(blogpost) %></td>
                <!--<%= link_to 'Delete', blogpost_path(blogpost), :method => :delete %>-->
            </p>
        <% end %>
    <% end %>
</div>


<%= render 'layouts/admin' if user_signed_in? %>